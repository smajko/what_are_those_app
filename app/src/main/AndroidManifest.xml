<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.smajko.watapp"
    android:versionCode="1"
    android:versionName="1.0" >


    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="org.smajko.watapp.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="org.smajko.watapp.AgeActivity"
            android:label="title_age_activity" >
        </activity>

        <activity
            android:name="org.smajko.watapp.SymptomActivity"
            android:label="title_symptom_activity" >
        </activity>

        <activity
            android:name="org.smajko.watapp.CropActivity"
            android:label="title_crop_activity" >
        </activity>
    </application>

</manifest>
